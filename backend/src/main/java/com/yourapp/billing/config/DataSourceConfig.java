package com.yourapp.billing.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfig {
    // Spring Boot Auto-configuration manages DataSource via application.properties.
    // Custom bean overrides can be configured here if multi-datasource or advanced pooling is required.
}
